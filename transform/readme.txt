report.pdf конвертировал в report.csv (самого процесса нет, т.к ждать 3-5 минут)

в файле main.py :
		- функция cut_csv() - обрезает файл report.csv и преобразует в list атрибутов(не по условию) 
		- функция	get_arr() - из list атрибутов(не по условию)  создает массив готовых атрибутов(по условию)
		- функция def get_csv() - создает датафрейм и записывает в файл credit_history.csv

в файле Credit_history.ipynb :
		- обработка, очистка
		- анализ
		- запись в credit_history_final.csv

  